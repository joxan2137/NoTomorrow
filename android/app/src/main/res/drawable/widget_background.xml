<?xml version="1.0" encoding="utf-8"?>
<!-- Home-screen widget surface (docs/widgets.md, "Look"): a near-black vertical gradient with a
     soft ember glow in the top-right corner and a hairline edge, so the widget reads as a lit
     object on the wallpaper rather than a flat slab. Rounded for launchers before API 31 (on 31+
     the system radius clips it, `GlanceModifier.cornerRadius`). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="@color/nt_widget_top"
                android:endColor="@color/nt_ground" />
            <corners android:radius="22dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="radial"
                android:centerX="1.0"
                android:centerY="0.0"
                android:gradientRadius="150dp"
                android:startColor="@color/nt_widget_glow"
                android:endColor="@color/nt_transparent" />
            <corners android:radius="22dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="@color/nt_widget_edge" />
            <corners android:radius="22dp" />
        </shape>
    </item>
</layer-list>
